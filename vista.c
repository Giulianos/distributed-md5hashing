//Vista.c
	/*Recibe por línea de comando el PID del proceso aplicación (corren en el mismo
sistema).
● Se conecta al proceso application, muestra y actualiza en pantalla el contenido del buffer
de llegada usando el formato: <nombre de archivo>: <hash md5> para cada archivo
procesado.
	*/


int main(int pid){
	//Obtengo estados de aplicacion
	


}
void printFormat(char[] pname, char[] hash){
	char[] salida = pname + ":" + hash + "\n";	
	printf(salida);
}
